/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'via.placeholder.com',
        'images.unsplash.com',
        'media.istockphoto.com',
        'plus.unsplash.com',
      ],
      },
};

export default nextConfig;
